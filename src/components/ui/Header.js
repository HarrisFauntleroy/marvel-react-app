import logo from '../../img/logo.png'

const Header = () => {

    return (
        <header>
            <img src={logo} alt="Marvel + React logo"></img>
        </header>
    )
}

export default Header
