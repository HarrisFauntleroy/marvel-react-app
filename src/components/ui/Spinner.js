import React from 'react'
import spinner from '../../img/spinner.gif'

const Spinner = () => {
    return (
        <div className="cards">
            <img
                src={spinner}
                alt="loading wheel">
            </img>
        </div>
    )
}

export default Spinner
